# Secure-Talent ATS (PWA)

Sistema avanzado de reclutamiento con procesamiento de IA, multi-idioma y encriptación de nivel bancario.

## 🚀 Instalación Rápida (Docker)

1. Clonar repositorio: `git clone ...`
2. Configurar `.env` (Incluir claves de Google Drive y OpenAI).
3. Levantar entorno:
   ```bash
   docker-compose up -d --build
